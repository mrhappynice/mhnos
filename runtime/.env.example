# MHNOS Runtime - Remote VPS Configuration
# Copy this file to .env and customize for your deployment

# =============================================================================
# VPS Configuration
# =============================================================================

# Your VPS IP address or domain name
VPS_HOST=your-vps-ip-or-domain.com

# WebSocket port for MHNOS browser connection
MHNOS_RUNTIME_PORT=18790

# =============================================================================
# MinIO S3 Configuration
# =============================================================================

# MinIO root credentials (change these!)
MINIO_ROOT_USER=mhnosadmin
MINIO_ROOT_PASSWORD=change-this-to-secure-password

# Public S3 endpoint (for presigned URLs)
# Use http:// for local/network, https:// for SSL
S3_PUBLIC_ENDPOINT=http://${VPS_HOST}:9000

# MinIO Console settings (optional)
# MINIO_BROWSER_REDIRECT_URL=https://console.your-domain.com

# =============================================================================
# OpenClaw Configuration
# =============================================================================

# Enable OpenClaw AI agent
INSTALL_OPENCLAW=true
MHNOS_ALLOW_OPENCLAW=true

# =============================================================================
# Runtime Settings
# =============================================================================

# Sandbox mode: strict, relaxed, none
MHNOS_SANDBOX=relaxed

# Logging: debug, info, warn, error
MHNOS_LOG_LEVEL=info

# Resource limits
MHNOS_MAX_PROCESSES=50
MHNOS_MAX_MEMORY=4g
RUNTIME_CPU_LIMIT=4.0
RUNTIME_MEMORY_LIMIT=8G
RUNTIME_CPU_RESERVE=1.0
RUNTIME_MEMORY_RESERVE=2G

# =============================================================================
# File Sync Settings
# =============================================================================

# Auto-sync on connect (not recommended for large workspaces)
SYNC_ON_CONNECT=false

# Patterns to exclude from sync (comma-separated)
SYNC_EXCLUDE_PATTERNS=node_modules,.git,dist,.cache,*.tmp

# Presigned URL expiry in seconds
S3_PRESIGN_EXPIRY=3600
